# 数据对齐优化集成检查清单

## 📋 集成前准备

- [ ] 已阅读 `README_PROJECT.md`（项目总览）
- [ ] 已阅读 `DIAGNOSIS_SUMMARY.md`（诊断结果）
- [ ] 已阅读 `INTEGRATION_GUIDE.md`（集成指南）
- [ ] 已运行诊断工具：`python diagnose_data_lag.py`
- [ ] 了解当前系统的数据滞后情况（特别是1h周期）
- [ ] 备份现有代码（建议使用git）

---

## 🔧 配置阶段

### 配置文件
- [ ] 确认 `config/data_alignment.yaml` 存在
- [ ] 选择合适的模式（backtest/live_safe/live_aggressive）
- [ ] 配置各周期的 `use_realtime` 设置
- [ ] 配置 `min_completion_pct`（最小完成度要求）
- [ ] 配置告警阈值（`lag_warning_threshold`）
- [ ] 验证YAML语法（缩进、冒号等）

### 推荐配置（live_safe）
```yaml
mode: 'live_safe'

timeframe_settings:
  5m:
    use_realtime: true
    min_completion_pct: 30
    lag_warning_threshold: 10
  
  15m:
    use_realtime: true
    min_completion_pct: 40
    lag_warning_threshold: 20
  
  1h:
    use_realtime: false
    lag_warning_threshold: 120
```

---

## 💻 代码集成

### 1. 核心修改：`src/features/builder.py`

- [ ] 导入 DataAlignmentHelper
  ```python
  from src.utils.data_alignment import DataAlignmentHelper
  ```

- [ ] 在 `__init__` 中初始化助手
  ```python
  def __init__(self, config):
      self.config = config
      self.alignment_helper = DataAlignmentHelper()
  ```

- [ ] 修改 `build_features()` 方法
  - [ ] 找到获取最新K线的代码（通常是 `df.iloc[-2]`）
  - [ ] 替换为：
    ```python
    latest, metadata = self.alignment_helper.get_aligned_candle(df, timeframe)
    ```
  - [ ] 添加日志记录：
    ```python
    self.logger.info(
        f"[{timeframe}] 数据时间: {metadata['timestamp'].strftime('%H:%M:%S')}, "
        f"滞后: {metadata['lag_minutes']:.1f}分钟, "
        f"模式: {'实时' if metadata['is_realtime'] else '滞后'}"
    )
    ```
  - [ ] 在返回的特征中包含时间戳：
    ```python
    features[timeframe] = {
        # ... 现有特征
        'timestamp': metadata['timestamp'],
        'lag_minutes': metadata['lag_minutes'],
        'is_realtime': metadata['is_realtime'],
    }
    ```

- [ ] 检查语法错误：`python -m py_compile src/features/builder.py`

### 2. 可选增强：`src/strategies/step4_multi_timeframe_score.py`

- [ ] 添加 `_log_timeframe_alignment()` 方法
  ```python
  def _log_timeframe_alignment(self, features: Dict[str, Dict]):
      timestamps = []
      for timeframe, feat in features.items():
          if 'timestamp' in feat:
              timestamps.append((
                  timeframe, 
                  feat['timestamp'], 
                  feat.get('lag_minutes', 0)
              ))
      
      # 显示时间状态
      timestamps.sort(key=lambda x: x[1], reverse=True)
      self.logger.info("多周期数据时间状态:")
      for tf, ts, lag in timestamps:
          self.logger.info(
              f"  [{tf:3s}] {ts.strftime('%Y-%m-%d %H:%M:%S')} "
              f"(滞后 {lag:5.1f}分钟)"
          )
      
      # 计算时间错位
      earliest = min([t[1] for t in timestamps])
      latest = max([t[1] for t in timestamps])
      time_gap = (latest - earliest).total_seconds() / 60
      
      self.logger.info(f"  时间错位: {time_gap:.1f}分钟")
      
      if time_gap > 60:
          self.logger.warning(
              f"  ⚠️ 时间错位超过1小时！可能影响决策准确性"
          )
  ```

- [ ] 在 `calculate()` 方法开头调用
  ```python
  def calculate(self, features: Dict[str, Dict]) -> Dict:
      self._log_timeframe_alignment(features)
      # ... 原有逻辑
  ```

### 3. 可选增强：`src/strategies/step6_integrated_signal.py`

- [ ] 添加 `_check_data_alignment()` 方法
  ```python
  def _check_data_alignment(self, features: Dict[str, Dict]):
      max_lag = 0
      for timeframe, feat in features.items():
          lag = feat.get('lag_minutes', 0)
          max_lag = max(max_lag, lag)
          
          if lag > 120:  # 超过2小时
              self.logger.warning(
                  f"🔴 [{timeframe}] 数据严重滞后: {lag:.1f}分钟"
              )
      
      # 记录到元数据（如果存在）
      if hasattr(self, 'signal_metadata'):
          self.signal_metadata['max_data_lag_minutes'] = max_lag
  ```

- [ ] 在 `generate_signal()` 方法中调用
  ```python
  def generate_signal(self, all_data: Dict) -> Dict:
      features = all_data.get('features', {})
      self._check_data_alignment(features)
      # ... 原有逻辑
  ```

### 4. 可选：主程序 `run_live_trading.py`

- [ ] 显示数据对齐配置
  ```python
  from src.utils.data_alignment import DataAlignmentHelper
  
  def main():
      alignment_helper = DataAlignmentHelper()
      logger.info(f"数据对齐模式: {alignment_helper.mode}")
      # ... 继续原有逻辑
  ```

---

## 🧪 测试与验证

### 单元测试
- [ ] 测试工具模块：`python src/utils/data_alignment.py`
- [ ] 检查语法错误（所有修改的文件）
- [ ] 运行已有的单元测试（如果有）

### 集成测试
- [ ] 使用小数据集测试 FeatureBuilder
- [ ] 确认能正确获取对齐的K线
- [ ] 确认日志中出现时间戳和滞后信息
- [ ] 确认不同模式（backtest/live_safe）行为正确

### 纸面交易测试
- [ ] 启动纸面交易：`python run_live_trading.py --mode paper`
- [ ] 运行至少1小时
- [ ] 观察日志中的数据时间戳
- [ ] 确认滞后时间符合预期
- [ ] 确认没有异常错误
- [ ] 检查是否有"信号闪烁"现象

### 性能测试
- [ ] 对比修改前后的执行时间
- [ ] 确认 DataAlignmentHelper 不会显著增加延迟（应该 <1ms）
- [ ] 检查内存使用是否正常

---

## 📊 回测验证

### 回测配置
- [ ] 设置 `mode: 'backtest'` （必须！避免未来函数）
- [ ] 准备测试数据（建议最近1-3个月）
- [ ] 记录原有策略的基准指标

### 对比回测
- [ ] 运行 baseline（mode: backtest，原有逻辑）
- [ ] 运行 live_safe 模式回测（需注意：结果可能与实盘不同）
- [ ] 对比关键指标：
  - [ ] 夏普比率
  - [ ] 最大回撤
  - [ ] 胜率
  - [ ] 盈亏比
  - [ ] 平均持仓时长
  - [ ] 交易次数

### 分析差异
- [ ] 记录两种模式的表现差异
- [ ] 分析差异的原因（入场时机、信号数量等）
- [ ] 评估是否符合预期

---

## 🚀 实盘部署

### 预部署检查
- [ ] 确认纸面交易测试通过（至少24小时无异常）
- [ ] 确认日志中数据滞后符合预期
- [ ] 确认没有频繁的告警
- [ ] 确认策略逻辑未被破坏（该买入时买入，该卖出时卖出）
- [ ] 准备回滚方案（git commit, 备份配置）

### 小仓位测试
- [ ] 设置测试仓位（建议10-20%总资金）
- [ ] 启动实盘交易
- [ ] 密切监控前2-4小时
  - [ ] 检查日志中的数据时间戳
  - [ ] 检查交易执行是否正常
  - [ ] 检查滞后告警
  - [ ] 检查盈亏情况

### 24小时监控
- [ ] 每4-6小时检查一次系统状态
- [ ] 记录关键指标：
  - [ ] 数据滞后时间（平均和最大）
  - [ ] 交易次数
  - [ ] 盈亏情况
  - [ ] 异常事件（如果有）
- [ ] 确认系统稳定运行

### 扩大规模
- [ ] 48小时测试无异常后，增加到30-50%仓位
- [ ] 继续监控48小时
- [ ] 评估表现是否优于原策略
- [ ] 根据实际情况决定是否全仓

---

## 📝 文档更新

### 代码注释
- [ ] 在修改的代码处添加注释说明
- [ ] 标注使用 DataAlignmentHelper 的原因
- [ ] 说明不同模式的适用场景

### 配置文档
- [ ] 更新项目配置文档（如果有）
- [ ] 说明 data_alignment.yaml 的作用
- [ ] 记录推荐配置和原因

### 运维文档
- [ ] 更新部署文档
- [ ] 添加诊断工具使用说明
- [ ] 记录常见问题和解决方案

---

## ⚠️ 风险检查

### 技术风险
- [ ] 确认回测模式不会引入未来函数（mode必须为'backtest'）
- [ ] 确认实时K线模式不会导致过度交易（检查信号频率）
- [ ] 确认告警阈值合理（不会漏报也不会频繁误报）

### 策略风险
- [ ] 评估策略类型是否适合实时模式
  - 突破/反转策略：✅ 适合
  - 趋势跟随策略：⚠️ 谨慎
  - 套利策略：✅ 适合
- [ ] 确认止损逻辑未受影响
- [ ] 确认仓位管理未受影响

### 运营风险
- [ ] 准备应急预案（如何快速回滚）
- [ ] 确保有足够的人力监控（至少前3天）
- [ ] 设置异常告警（如滞后超过阈值、交易失败等）

---

## 📞 问题排查清单

### 配置未生效
- [ ] 检查 `config/data_alignment.yaml` 是否存在
- [ ] 检查 YAML 语法（缩进、冒号）
- [ ] 检查 DataAlignmentHelper 初始化日志
- [ ] 打印 `helper.config` 和 `helper.mode` 确认加载正确

### 滞后仍然很高
- [ ] 确认配置中 `use_realtime: true`
- [ ] 确认 `min_completion_pct` 不会太高（导致降级使用 iloc[-2]）
- [ ] 检查网络延迟
- [ ] 检查数据源响应时间

### 信号闪烁（频繁买卖）
- [ ] 提高 `min_completion_pct`（30% → 50%）
- [ ] 在策略中增加信号确认机制（连续N次）
- [ ] 设置最小持仓时间
- [ ] 考虑切换到 `live_safe` 或 `backtest` 模式

### 异常错误
- [ ] 检查完整的错误堆栈
- [ ] 确认所有依赖已安装（PyYAML等）
- [ ] 检查日志中的错误信息
- [ ] 运行诊断工具确认数据正常

---

## ✅ 最终验收

### 功能验收
- [ ] 不同模式（backtest/live_safe/live_aggressive）均可正常工作
- [ ] 数据滞后符合预期（见配置模式对比表）
- [ ] 时间戳日志正常输出
- [ ] 滞后告警机制正常工作
- [ ] 时间错位检测正常工作

### 性能验收
- [ ] 执行时间无显著增加（<5%）
- [ ] 内存使用正常
- [ ] 无内存泄漏

### 交易验收
- [ ] 回测结果合理（与baseline对比）
- [ ] 纸面交易表现正常（24小时+）
- [ ] 小仓位实盘表现符合预期（48小时+）
- [ ] 盈亏曲线平滑（无异常波动）

---

## 📚 后续优化

### 短期（1-2周）
- [ ] 根据实盘表现微调配置
- [ ] 优化 `min_completion_pct` 参数
- [ ] 优化告警阈值
- [ ] 收集用户反馈

### 中期（1-2个月）
- [ ] 评估不同策略类型的适配性
- [ ] 开发自适应模式（根据市场波动率自动切换）
- [ ] 增加更多诊断指标
- [ ] 完善监控面板

### 长期（3-6个月）
- [ ] 开发Web配置界面
- [ ] 支持更多交易所
- [ ] 集成到统一监控平台
- [ ] 机器学习优化参数选择

---

## 📋 签名确认

### 集成负责人
- 姓名: _________________
- 日期: _________________
- 签名: _________________

### 测试负责人
- 姓名: _________________
- 日期: _________________
- 签名: _________________

### 部署负责人
- 姓名: _________________
- 日期: _________________
- 签名: _________________

---

**清单版本**: v1.0  
**最后更新**: 2025-12-18  
**相关文档**: README_PROJECT.md, INTEGRATION_GUIDE.md
